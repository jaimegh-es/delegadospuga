<section class="features-section" id="funciones">
	<div class="container">
		<div class="section-header">
			<h2 class="section-title"><i class="fas fa-bullseye"></i>Has elegido bien</h2>
			<p class="section-subtitle">Somos tu voz ante la dirección del centro, y nos van a oír</p>
		</div>

		<div class="features-grid">
			<div class="feature-card">
				<div class="feature-icon">
					<div class="icon-3d"><i class="fas fa-bullhorn"></i></div>
				</div>
				<h3>Alto y claro</h3>
				<p>Tus sugerencias serán presentadas ante la persona correspondiente.</p>
			</div>

			<div class="feature-card">
				<div class="feature-icon">
					<div class="icon-3d"><i class="fas fa-calendar-alt"></i></div>
				</div>
				<h3>Organización</h3>
				<p>Reparto de circulares, boletines... Siempre recibirás lo que nos entreguen para tí</p>
			</div>

			<div class="feature-card">
				<div class="feature-icon">
					<div class="icon-3d"><i class="fa-solid fa-chart-line"></i></div>
				</div>
				<h3>Mejoras, por favor</h3>
				<p>Impulsaremos todas las mejoras que creas convenientes.</p>
			</div>

			<div class="feature-card">
				<div class="feature-icon">
					<div class="icon-3d"><i class="fas fa-handshake"></i></div>
				</div>
				<h3>Estrechar lazos, mejorar en común</h3>
				<p>Estrechando lazos somos más fuertes y podemos hacer valer nuestra opinión.</p>
			</div>

			<div class="feature-card">
				<div class="feature-icon">
					<div class="icon-3d"><i class="fa-regular fa-face-smile-wink"></i></div>
				</div>
				<h3>Siempre abiertos</h3>
				<p>Tienes algo en mente que crees necesario solicitar: aquí estoy.</p>
			</div>

			<div class="feature-card">
				<div class="feature-icon">
					<div class="icon-3d"><i class="fa-solid fa-user-tie"></i></div>
				</div>
				<h3>Decencia, por encima de todo</h3>
				<p>Hablemos como personas, así defenderemos mejor nuestros intereses.</p>
			</div>
		</div>

		<!-- <div class="stats-section">
			<div class="stat-item">
				<div class="stat-number">25+</div>
				<div class="stat-label">Propuestas Realizadas</div>
			</div>
			<div class="stat-item">
				<div class="stat-number">12</div>
				<div class="stat-label">Eventos Organizados</div>
			</div>
			<div class="stat-item">
				<div class="stat-number">100%</div>
				<div class="stat-label">Participación Activa</div>
			</div>
		</div>-->
	</div>
</section>

<style>
	.features-section {
		padding: 120px 0;
		background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%);
		background: linear-gradient(135deg, #001e60 0%, #000d33 100%);
		color: white;
		position: relative;
		overflow: hidden;
	}

	.features-section::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
		opacity: 0.1;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 24px;
		position: relative;
		z-index: 1;
	}

	.section-header {
		text-align: center;
		margin-bottom: 80px;
	}

	.section-title {
		font-size: 3rem;
		font-weight: 700;
		margin-bottom: 16px;
		background: linear-gradient(45deg, #ffffff, #ffffff);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.section-subtitle {
		font-size: 1.125rem;
		color: #94a3b8;
		font-weight: 400;
	}

	.features-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
		gap: 32px;
		margin-bottom: 80px;
	}

	.feature-card {
		background: rgba(255, 255, 255, 0.05);
		border-radius: 24px;
		padding: 40px 32px;
		text-align: center;
		backdrop-filter: blur(10px);
		border: 1px solid rgba(255, 255, 255, 0.1);
		transition: all 0.4s ease;
		transform: perspective(1000px);
	}

	.feature-card:hover {
		transform: perspective(1000px) translateY(-10px) rotateY(5deg);
		background: rgba(255, 255, 255, 0.08);
		border-color: rgba(255, 255, 255, 0.3);
		box-shadow: 0 20px 60px rgba(255, 255, 255, 0.2);
	}

	.feature-icon {
		margin-bottom: 24px;
		display: flex;
		justify-content: center;
	}

	.icon-3d {
		font-size: 3rem;
		display: flex;
		align-items: center;
		justify-content: center;
		width: 80px;
		height: 80px;
		background: linear-gradient(45deg, #ffffff, #ffffff);
		color: #001e60;
		border-radius: 20px;
		transform-style: preserve-3d;
		transition: all 0.4s ease;
		box-shadow: 0 8px 32px rgba(255, 255, 255, 0.3);
	}

	.feature-card:hover .icon-3d {
		transform: rotateY(180deg) rotateX(15deg);
		box-shadow: 0 12px 48px rgba(255, 255, 255, 0.5);
	}

	.feature-card h3 {
		font-size: 1.5rem;
		font-weight: 600;
		margin-bottom: 16px;
		color: white;
	}

	.feature-card p {
		color: #94a3b8;
		line-height: 1.6;
	}

	.stats-section {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 40px;
		text-align: center;
		padding: 60px 0;
		border-top: 1px solid rgba(255, 255, 255, 0.1);
	}

	.stat-item {
		transform: perspective(1000px);
		transition: all 0.3s ease;
	}

	.stat-item:hover {
		transform: perspective(1000px) rotateX(10deg) scale(1.05);
	}

	.stat-number {
		font-size: 3rem;
		font-weight: 700;
		background: linear-gradient(45deg, #ffffff, #ffffff);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin-bottom: 8px;
	}

	.stat-label {
		color: #94a3b8;
		font-weight: 500;
		text-transform: uppercase;
		font-size: 0.875rem;
		letter-spacing: 0.05em;
	}

	@media (max-width: 768px) {
		.features-section {
			padding: 80px 0;
		}

		.section-title {
			font-size: 2rem;
		}

		.features-grid {
			grid-template-columns: 1fr;
			gap: 24px;
		}

		.feature-card {
			padding: 32px 24px;
		}

		.icon-3d {
			width: 60px;
			height: 60px;
			font-size: 2rem;
		}

		.stats-section {
			grid-template-columns: 1fr;
			gap: 24px;
		}

		.stat-number {
			font-size: 2rem;
		}
	}
</style>