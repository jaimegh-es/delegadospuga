---
---

<link href="https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.snow.css" rel="stylesheet" />

<div class="admin-card">
    <h3>Gestión de Eventos</h3>

    <!-- Formulario Eventos -->
    <div id="event-manager-content">
        <h4 id="event-form-title" class="form-title">Añadir Evento</h4>
        <form id="event-form" class="event-form">
            <input type="hidden" name="id" id="event-id">
            <input type="hidden" name="type" value="event">
            
            <div class="form-group">
                <label>Fecha:</label>
                <input type="date" name="date" id="event-date" required>
            </div>

            <div class="form-group">
                <label>Título del Evento:</label>
                <input type="text" name="title" id="event-title" required placeholder="Ej: Excursión, Festivo...">
            </div>

             <div class="form-group">
                <label>Hora (Opcional):</label>
                <input type="text" name="time" id="event-time" placeholder="Ej: Todo el día">
            </div>

            <div class="form-group full-width">
                <label>Detalles:</label>
                <div id="editor-events" class="quill-editor"></div>
                <input type="hidden" name="details" id="hidden-details-events">
            </div>

            <div class="form-actions">
                <button type="button" id="cancel-event-edit" class="secondary-btn hidden">Cancelar Edición</button>
                <button type="submit" class="submit-btn" id="event-submit-btn">Añadir Evento</button>
            </div>
        </form>
    </div>

    <!-- Vista Calendario -->
    <div class="calendar-preview-section">
        <div class="section-header-preview">
            <h4>Vista Previa Eventos</h4>
            <p class="hint-preview">Pasa el ratón para editar o borrar</p>
        </div>
        
        <div class="month-controls">
            <button id="prev-month-events">&lt;</button>
            <h3 id="current-month-display-events" class="mes-titulo"></h3>
            <button id="next-month-events">&gt;</button>
        </div>
        
        <div class="mes-container">
            <div class="mes">
                <table class="calendario-tabla">
                    <thead>
                        <tr>
                            <th>L</th>
                            <th>M</th>
                            <th>X</th>
                            <th>J</th>
                            <th>V</th>
                            <th>S</th>
                            <th>D</th>
                        </tr>
                    </thead>
                    <tbody id="admin-event-calendar-body">
                        <!-- Filled by JS -->
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Bottom Bar for Actions -->
    <div id="mobile-action-bar-events" class="bottom-action-bar">
        <div class="selected-info">
            <span id="selected-event-name"></span>
        </div>
        <div class="action-buttons">
            <button id="bar-edit-btn-events" class="bar-edit-btn">
                <i class="fa-solid fa-pen-to-square"></i> Editar
            </button>
            <button id="bar-delete-btn-events" class="bar-delete-btn">
                <i class="fa-solid fa-trash"></i> Borrar
            </button>
            <button id="bar-close-btn-events" class="bar-close-btn">
                <i class="fa-solid fa-xmark"></i>
            </button>
        </div>
    </div>

    <!-- Selection Modal -->
    <div id="selection-modal-events" class="modal-selection-overlay hidden">
        <div class="modal-selection-content">
            <div class="modal-selection-header">
                <h4>Eventos del día</h4>
                <button id="close-selection-modal-events">&times;</button>
            </div>
            <div id="selection-list-events" class="selection-list">
                <!-- Filled by JS -->
            </div>
        </div>
    </div>
</div>

<style>
    /* Selection Modal Styles */
    .modal-selection-overlay {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0,0,0,0.5);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 3000;
        padding: 1rem;
    }

    .modal-selection-content {
        background: white;
        border-radius: 12px;
        width: 100%;
        max-width: 400px;
        overflow: hidden;
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
    }

    .modal-selection-header {
        padding: 1rem;
        border-bottom: 1px solid #e2e8f0;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .modal-selection-header h4 {
        margin: 0;
        color: #1e293b;
    }

    .modal-selection-header button {
        background: none;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        color: #64748b;
    }

    .selection-list {
        max-height: 300px;
        overflow-y: auto;
    }

    :global(.selection-item) {
        padding: 1rem;
        border-bottom: 1px solid #f1f5f9;
        cursor: pointer;
        transition: background 0.2s;
        display: flex;
        flex-direction: column;
        gap: 4px;
    }

    :global(.selection-item:hover) {
        background: #f8fafc;
    }

    :global(.selection-item .item-title) {
        font-weight: 700;
        color: #10b981;
    }

    :global(.selection-item .item-time) {
        font-size: 0.85rem;
        color: #64748b;
    }

    /* Bottom Action Bar enhancements */
    .bottom-action-bar {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        background: white;
        box-shadow: 0 -4px 15px rgba(0,0,0,0.1);
        padding: 1rem;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        z-index: 2000;
        transform: translateY(100%);
        transition: transform 0.3s ease;
        border-top: 1px solid #e2e8f0;
    }

    .bottom-action-bar.active {
        transform: translateY(0);
    }

    .selected-info {
        text-align: center;
        font-weight: 600;
        color: #475569;
        font-size: 0.9rem;
        border-bottom: 1px solid #f1f5f9;
        padding-bottom: 0.5rem;
    }

    .action-buttons {
        display: flex;
        gap: 1rem;
    }
    
    .action-buttons button {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        padding: 0.75rem;
        border-radius: 8px;
        font-weight: 700;
        border: none;
        cursor: pointer;
    }

    .bar-edit-btn { background: #10b981; color: white; }
    .bar-delete-btn { background: #fee2e2; color: #ef4444; }
    .bar-close-btn { background: #f1f5f9; color: #475569; max-width: 50px; }

    .admin-card {
        background: white;
        padding: 1.5rem;
        border-radius: 12px;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        border: 1px solid #e2e8f0;
    }

    h3 {
        color: #10b981; /* Green for events */
        font-weight: 700;
        margin-bottom: 1.5rem;
    }

    .form-title {
        font-size: 1.125rem;
        font-weight: 600;
        color: #475569;
        margin-bottom: 1rem;
    }

    .section-header-preview {
        margin-top: 2rem;
        margin-bottom: 1rem;
        border-top: 1px solid #e2e8f0;
        padding-top: 1rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    h4 {
        color: #475569;
        margin: 0;
    }

    .hint-preview {
        font-size: 0.8rem;
        color: #64748b;
        font-weight: 600;
    }

    .event-form {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 16px;
    }

    @media (max-width: 640px) {
        .event-form {
            grid-template-columns: 1fr;
        }
    }

    .form-group {
        display: flex;
        flex-direction: column;
        gap: 6px;
    }

    .form-group.full-width {
        grid-column: 1 / -1;
    }

    label {
        font-size: 0.875rem;
        font-weight: 600;
        color: #475569;
    }

    input {
        padding: 8px 12px;
        border: 1px solid #cbd5e1;
        border-radius: 6px;
        font-size: 0.95rem;
    }

    .bottom-action-bar {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        background: white;
        box-shadow: 0 -4px 15px rgba(0,0,0,0.1);
        padding: 1rem;
        display: flex;
        gap: 1rem;
        z-index: 2000;
        transform: translateY(100%);
        transition: transform 0.3s ease;
        border-top: 1px solid #e2e8f0;
    }

    .bottom-action-bar.active {
        transform: translateY(0);
    }

    .bottom-action-bar button {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        padding: 0.75rem;
        border-radius: 8px;
        font-weight: 700;
        border: none;
        cursor: pointer;
    }

    .bar-edit-btn { background: #10b981; color: white; }
    .bar-delete-btn { background: #fee2e2; color: #ef4444; }
    .bar-close-btn { background: #f1f5f9; color: #475569; max-width: 50px; }

    .quill-editor {
        height: 150px;
        background: white;
        border-bottom-left-radius: 6px;
        border-bottom-right-radius: 6px;
    }

    .form-actions {
        grid-column: 1 / -1;
        display: flex;
        justify-content: flex-end;
        gap: 10px;
        margin-top: 10px;
    }

    .submit-btn {
        background: #10b981;
        color: white;
        border: none;
        padding: 12px 24px;
        border-radius: 8px;
        font-weight: 600;
        cursor: pointer;
    }
    .submit-btn:hover {
        background: #059669;
    }

    .secondary-btn {
        background: #e2e8f0;
        color: #475569;
        border: none;
        padding: 12px 24px;
        border-radius: 8px;
        font-weight: 600;
        cursor: pointer;
    }

    .hidden {
        display: none !important;
    }

    /* CALENDAR STYLES */
    .month-controls {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 20px;
        margin-bottom: 20px;
    }

    .month-controls button {
        background: #f1f5f9;
        border: none;
        width: 30px;
        height: 30px;
        border-radius: 50%;
        cursor: pointer;
        font-weight: bold;
        color: #10b981;
    }

    .mes-container {
        max-width: 600px;
        margin: 0 auto;
    }

    .mes {
        background: #f8fafc;
        border-radius: 16px;
        padding: 24px;
        border: 1px solid #e2e8f0;
    }

    .mes-titulo {
        font-size: 1.5rem;
        font-weight: 600;
        color: #10b981;
        margin: 0;
        text-align: center;
    }

    .calendario-tabla {
        width: 100%;
        border-collapse: collapse;
        border: 2px solid #e2e8f0;
        border-radius: 12px;
        overflow: hidden;
    }

    .calendario-tabla th {
        background: linear-gradient(45deg, #10b981, #059669);
        color: white;
        padding: 12px 8px;
        text-align: center;
        font-weight: 600;
        font-size: 0.875rem;
    }

    :global(.calendario-tabla td) {
        width: 14.28%;
        height: 80px;
        border: 1px solid #e2e8f0;
        position: relative;
        vertical-align: top;
        padding: 4px;
        cursor: pointer;
        transition: background 0.2s;
    }

    :global(.calendario-tabla td:hover:not(.dia-vacio)) {
        background: #f1f5f9;
    }

    :global(.dia.tiene-evento) {
        background: #ecfdf5;
        border-top: 3px solid #10b981 !important;
    }

    :global(.dia.tiene-evento.selected) {
        background: #10b981;
        color: white;
    }

    :global(.numero-dia) {
        position: absolute;
        top: 4px;
        left: 6px;
        font-weight: 600;
        font-size: 0.875rem;
    }

    :global(.examen-info) {
        position: absolute;
        bottom: 4px;
        left: 4px;
        right: 4px;
        text-align: center;
        font-size: 0.6rem;
        line-height: 1.1;
        pointer-events: none;
    }

    :global(.examen-info .asignatura) {
        font-weight: 700;
        text-transform: uppercase;
    }
</style>

<script>
    import Quill from 'quill';

    let currentDate = new Date();
    let eventsData = [];
    let quillEvents;

    function initQuill() {
        const toolbarOptions = [
            ['bold', 'italic', 'underline', 'strike'],
            [{ 'list': 'ordered'}, { 'list': 'bullet' }],
            [{ 'header': [1, 2, 3, false] }],
            [{ 'color': [] }],
            ['link', 'clean']
        ];

        quillEvents = new Quill('#editor-events', {
            theme: 'snow',
            placeholder: 'Detalles del evento...',
            modules: { toolbar: toolbarOptions }
        });
    }

    async function loadEvents() {
        try {
            const res = await fetch('/api/calendar');
            const allData = await res.json();
            eventsData = allData.filter(e => e.type === 'event');
            renderCalendar();
        } catch (e) {
            console.error("Error loading events", e);
        }
    }

    function renderCalendar() {
        const tbody = document.getElementById('admin-event-calendar-body');
        const monthDisplay = document.getElementById('current-month-display-events');
        
        if (!tbody || !monthDisplay) return;

        const year = currentDate.getFullYear();
        const month = currentDate.getMonth();

        monthDisplay.textContent = new Date(year, month).toLocaleDateString('es-ES', { month: 'long', year: 'numeric' });

        const daysInMonth = new Date(year, month + 1, 0).getDate();
        const firstDay = new Date(year, month, 1).getDay();
        const adjustedFirstDay = firstDay === 0 ? 6 : firstDay - 1;

        tbody.innerHTML = '';
        
        let currentDay = 1;
        let row = document.createElement('tr');
        let dayCount = 0;

        for (let i = 0; i < adjustedFirstDay; i++) {
            const cell = document.createElement('td');
            cell.className = 'dia-vacio';
            row.appendChild(cell);
            dayCount++;
        }

        while (currentDay <= daysInMonth) {
            if (dayCount === 7) {
                tbody.appendChild(row);
                row = document.createElement('tr');
                dayCount = 0;
            }

            const dateStr = `${year}-${String(month + 1).padStart(2, '0')}-${String(currentDay).padStart(2, '0')}`;
            const dayEvents = eventsData.filter(e => e.date === dateStr);
            const hasEvent = dayEvents.length > 0;
            
            const cell = document.createElement('td');
            cell.className = hasEvent ? 'dia tiene-evento' : 'dia';
            cell.dataset.date = dateStr;

            cell.addEventListener('click', () => handleDayClick(dateStr));

            let content = `<div class="numero-dia">${currentDay}</div>`;
            
            if (hasEvent) {
                 content += `<div class="examen-info">`;
                 dayEvents.slice(0, 2).forEach(ev => {
                     content += `<div class="asignatura">${ev.title}</div>`;
                 });
                 if (dayEvents.length > 2) content += `<div class="asignatura">+${dayEvents.length - 2}</div>`;
                 content += `</div>`;
            }

            cell.innerHTML = content;
            row.appendChild(cell);
            currentDay++;
            dayCount++;
        }

        while (dayCount < 7 && dayCount > 0) {
            const cell = document.createElement('td');
            cell.className = 'dia-vacio';
            row.appendChild(cell);
            dayCount++;
        }
        if (row.children.length > 0) tbody.appendChild(row);
    }

    let selectedId = null;
    const mobileBar = document.getElementById('mobile-action-bar-events');
    const selectionModal = document.getElementById('selection-modal-events');
    const selectionList = document.getElementById('selection-list-events');

    function handleDayClick(dateStr) {
        const dayEvents = eventsData.filter(e => e.date === dateStr);
        if (dayEvents.length === 0) {
            closeMobileActionBar();
            return;
        }

        if (dayEvents.length === 1) {
            selectItem(dayEvents[0].id);
        } else {
            showSelectionList(dayEvents);
        }
    }

    function showSelectionList(events) {
        selectionList.innerHTML = '';
        events.forEach(ev => {
            const item = document.createElement('div');
            item.className = 'selection-item';
            item.innerHTML = `
                <span class="item-title">${ev.title}</span>
                <span class="item-time">${ev.time || 'Todo el día'}</span>
            `;
            item.onclick = () => {
                selectItem(ev.id);
                closeSelectionModal();
            };
            selectionList.appendChild(item);
        });
        selectionModal.classList.remove('hidden');
    }

    function closeSelectionModal() {
        selectionModal.classList.add('hidden');
    }

    function selectItem(id) {
        selectedId = id;
        const item = eventsData.find(e => e.id === id);
        if (!item) return;

        document.getElementById('selected-event-name').textContent = item.title;
        mobileBar.classList.add('active');

        // Highlight in calendar
        document.querySelectorAll('.dia').forEach(d => d.classList.remove('selected'));
        const cell = document.querySelector(`td[data-date="${item.date}"]`);
        if (cell) cell.classList.add('selected');
    }

    function closeMobileActionBar() {
        mobileBar.classList.remove('active');
        selectedId = null;
        document.querySelectorAll('.dia').forEach(d => d.classList.remove('selected'));
    }

    (window as any).editEventItem = (id) => {
        const item = eventsData.find(e => e.id === id);
        if (!item) return;

        (document.getElementById('event-id') as HTMLInputElement).value = item.id;
        (document.getElementById('event-date') as HTMLInputElement).value = item.date;
        (document.getElementById('event-title') as HTMLInputElement).value = item.title;
        (document.getElementById('event-time') as HTMLInputElement).value = item.time || '';
        quillEvents.root.innerHTML = item.details || '';

        document.getElementById('event-form-title').textContent = "Editar Evento";
        document.getElementById('event-submit-btn').textContent = "Actualizar Evento";
        document.getElementById('cancel-event-edit').classList.remove('hidden');
        
        window.scrollTo({ top: document.getElementById('event-manager-content').offsetTop - 100, behavior: 'smooth' });
        closeMobileActionBar();
    };

    function resetEventsForm() {
        const form = document.getElementById('event-form') as HTMLFormElement;
        form.reset();
        (document.getElementById('event-id') as HTMLInputElement).value = '';
        quillEvents.setContents([]);
        document.getElementById('event-form-title').textContent = "Añadir Evento";
        document.getElementById('event-submit-btn').textContent = "Añadir Evento";
        document.getElementById('cancel-event-edit').classList.add('hidden');
    }

    async function deleteEventItem(id) {
        if (!confirm("¿Borrar este evento?")) return;

        try {
            const res = await fetch('/api/calendar', {
                method: 'DELETE',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ id })
            });

            if (res.ok) {
                closeMobileActionBar();
                loadEvents();
            } else {
                alert("Error al borrar");
            }
        } catch (e) {
            alert("Error de conexión");
        }
    }
    (window as any).deleteEventItem = deleteEventItem;

    async function handleFormSubmit(e) {
        e.preventDefault();
        const form = e.target;
        document.getElementById('hidden-details-events').value = quillEvents.root.innerHTML;

        const formData = new FormData(form);
        const data = Object.fromEntries(formData.entries());
        
        try {
            const res = await fetch('/api/calendar', {
                method: data.id ? 'PATCH' : 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(data)
            });

            if (res.ok) {
                resetEventsForm();
                loadEvents();
                alert("Evento guardado correctamente");
            } else {
                alert("Error al guardar");
            }
        } catch (e) {
            alert("Error de conexión");
        }
    }

    document.addEventListener('DOMContentLoaded', () => {
        initQuill();
        loadEvents();
        document.getElementById('event-form')?.addEventListener('submit', handleFormSubmit);
        document.getElementById('cancel-event-edit')?.addEventListener('click', resetEventsForm);
        document.getElementById('prev-month-events')?.addEventListener('click', () => { currentDate.setMonth(currentDate.getMonth() - 1); renderCalendar(); });
        document.getElementById('next-month-events')?.addEventListener('click', () => { currentDate.setMonth(currentDate.getMonth() + 1); renderCalendar(); });

        document.getElementById('bar-edit-btn-events')?.addEventListener('click', () => {
            if (selectedId) (window as any).editEventItem(selectedId);
        });
        document.getElementById('bar-delete-btn-events')?.addEventListener('click', () => {
            if (selectedId) deleteEventItem(selectedId);
        });
        document.getElementById('bar-close-btn-events')?.addEventListener('click', closeMobileActionBar);
        document.getElementById('close-selection-modal-events')?.addEventListener('click', closeSelectionModal);
    });
</script>
